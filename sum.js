/**
 * Somma due numeri
 * @param {number} a - Primo numero
 * @param {number} b - Secondo numero
 * @returns {number} La somma di a e b
 */
function sum(a, b) {
  return a + b;
}

module.exports = sum;
